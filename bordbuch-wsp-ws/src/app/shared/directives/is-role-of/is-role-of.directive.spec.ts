import { IsRoleOfDirective } from './is-role-of.directive';

describe('IsRoleOfDirective', () => {
  it('should create an instance', () => {
    // const directive = new IsRoleOfDirective();
    // expect(directive).toBeTruthy();
  });
});
